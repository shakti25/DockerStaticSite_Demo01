version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: static-site-demo:latest
    container_name: static-site-demo
    ports:
      - "8080:80"
    restart: always
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
